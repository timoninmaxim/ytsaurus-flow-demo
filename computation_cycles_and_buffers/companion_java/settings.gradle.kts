// Standalone Gradle build for the Java-companion variant of the computation_cycles_and_buffers
// scenario. The Flow Java SDK (tech.ytsaurus:flow-*) comes from Maven: the released versions
// from Maven Central, the test releases (X.Y.Z-SNAPSHOT) from the Sonatype snapshot repository —
// see build.gradle.kts.
rootProject.name = "computation-cycles-java"
